import * as UserListNode from "./UserListNode.svelte";
import * as ModListNode from "./ModListNode.svelte";

export const listNodes = {
	userlist: UserListNode,
	modList: ModListNode,
};
export { UserListNode, ModListNode };
